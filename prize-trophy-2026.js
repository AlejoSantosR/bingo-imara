/* Bingo IMARA · Premio de ronda + carrusel público 2026 */
(function(){
'use strict';
const IS_PUBLIC=location.hash.startsWith('#public');
const IS_MOBILE=location.hash.startsWith('#mobile=');
if(IS_MOBILE)return;
const esc=s=>typeof escapeHtml==='function'?escapeHtml(String(s??'')):String(s??'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot',"'":'&#039;'}[m]));
const shortMoney=n=>{n=Number(n)||0;return n>=1000?`$${Math.round(n/1000)}K`:`$${n}`;};
function installCss(){if(document.getElementById('imaraPrizeTrophyCss'))return;const s=document.createElement('style');s.id='imaraPrizeTrophyCss';s.textContent=`
 #publicPrizeGallery{display:none!important}
 #roundPrizeSelectorWrap{margin-top:12px!important}
 #view-game #roundPrize,#view-game #prizeReveal,#view-game #roundStatus{display:none!important}
 #view-game #roundPrize:where(input),#view-game #prizeReveal:where(select),#view-game #roundStatus:where(select){display:none!important}
 .prize-hide-label{display:none!important}
 .prize-trophy-admin,.prize-trophy-public{position:relative;overflow:hidden;border:1px solid rgba(255,210,92,.32);background:linear-gradient(145deg,rgba(44,35,21,.96),rgba(28,24,43,.96));box-shadow:0 16px 45px rgba(0,0,0,.28),inset 0 0 35px rgba(255,196,72,.05)}
 .prize-trophy-admin{margin-top:12px;padding:12px;border-radius:17px;display:grid;grid-template-columns:82px 1fr;gap:12px;align-items:center}.prize-trophy-public{margin-top:10px;padding:14px;border-radius:22px;display:grid;grid-template-columns:98px 1fr;gap:14px;align-items:center;text-align:left}
 .prize-ring{width:76px;height:76px;border-radius:50%;display:grid;place-items:center;background:radial-gradient(circle at 35% 28%,#ffe99a,#e5ad2c 68%,#80530c);box-shadow:0 0 0 5px rgba(255,211,93,.08),0 8px 24px rgba(0,0,0,.34);overflow:hidden}.prize-trophy-public .prize-ring{width:92px;height:92px}.prize-ring img{width:80%;height:80%;object-fit:contain;background:transparent;border:0;border-radius:0}.prize-ring .gift{font-size:38px}.prize-trophy-public .gift{font-size:44px}
 .prize-trophy-label{font-size:9px;letter-spacing:1.7px;font-weight:1000;color:#ffd978;text-transform:uppercase}.prize-trophy-title{font-size:18px;font-weight:1000;margin-top:3px;color:#fff}.prize-trophy-public .prize-trophy-title{font-size:clamp(21px,3vw,30px)}.prize-trophy-desc{font-size:11px;color:var(--muted);margin-top:3px}.prize-trophy-icon{position:absolute;right:10px;top:6px;font-size:28px;opacity:.1;transform:rotate(10deg)}
 .public-price-v2{margin:14px 0 8px;padding:11px 13px;border-radius:16px;border:1px solid rgba(255,216,99,.3);background:linear-gradient(135deg,rgba(255,190,48,.12),rgba(141,107,255,.08));font-weight:1000;font-size:clamp(14px,2vw,20px)}.public-price-v2 strong{color:#ffe18c}
 .awarded-prizes{margin-top:12px;text-align:left}.awarded-prizes-title{font-size:9px;letter-spacing:1.7px;font-weight:1000;color:#aab2c3;margin-bottom:7px}.awarded-track{display:flex;gap:8px;overflow-x:auto;padding-bottom:5px;scrollbar-width:thin}.awarded-card{flex:0 0 190px;display:grid;grid-template-columns:46px 1fr;gap:8px;align-items:center;padding:9px;border:1px solid #374055;border-radius:13px;background:#151b29;filter:grayscale(1);opacity:.62}.awarded-card .prize-ring{width:42px;height:42px;box-shadow:none}.awarded-card .prize-ring img{width:76%;height:76%}.awarded-card b{font-size:11px;color:#e2e5ea}.awarded-card small{display:block;margin-top:2px;color:#aab2c1;font-size:9px}
 .winner-prize-moment{margin-top:14px;padding:18px;border-radius:24px;display:grid;grid-template-columns:120px 1fr;gap:18px;align-items:center;text-align:left;background:radial-gradient(circle at 15% 30%,rgba(255,210,75,.22),transparent 35%),linear-gradient(135deg,#2b2047,#171f35);border:1px solid rgba(255,215,102,.45);box-shadow:0 22px 65px rgba(0,0,0,.38),0 0 50px rgba(255,190,63,.13)}
 .winner-prize-moment img{width:116px;height:116px;object-fit:contain;background:transparent}.winner-prize-moment .winner-gift{width:116px;height:116px;border-radius:50%;display:grid;place-items:center;font-size:58px;background:radial-gradient(circle at 35% 28%,#ffe99a,#e5ad2c 68%,#80530c)}.winner-prize-name{font-size:clamp(23px,3vw,38px);font-weight:1000;line-height:1.05}.winner-prize-award{margin-top:7px;color:#ffe09a;font-size:17px;font-weight:900}.winner-prize-card{margin-top:5px;color:#c9d2e7;font-size:13px}
 @media(max-width:700px){.winner-prize-moment{grid-template-columns:82px 1fr}.winner-prize-moment img,.winner-prize-moment .winner-gift{width:80px;height:80px}.prize-trophy-public{grid-template-columns:72px 1fr}.prize-trophy-public .prize-ring{width:68px;height:68px}.prize-trophy-admin{grid-template-columns:68px 1fr}.prize-trophy-admin .prize-ring{width:62px;height:62px}}
 `;document.head.appendChild(s);}
function prizeById(id){return typeof state!=='undefined'&&Array.isArray(state.prizes)?state.prizes.find(p=>p.id===id):null;}
function info(){if(typeof state==='undefined')return {title:'Premio de la ronda',description:'',image:'',showPublic:true};const p=prizeById(state.round?.prizeId);return {id:p?.id||'',title:state.round?.prizeTitle||p?.title||state.round?.prize||'Premio de la ronda',description:state.round?.prizeDescription||p?.description||'',image:state.round?.prizeImage||p?.image||'',showPublic:p?.showPublic!==false};}
function persistPrize(){if(typeof state==='undefined')return;const id=document.getElementById('roundPrizeSelect')?.value||state.round?.prizeId||'';const p=prizeById(id);if(!p)return;const i=(state.prizes||[]).findIndex(x=>x.id===p.id);const ord=typeof ordinalPrizeLabel==='function'?ordinalPrizeLabel(i):`Premio ${i+1}`;state.round.prizeId=p.id;state.round.prizeTitle=p.title||'';state.round.prizeDescription=p.description||'';state.round.prizeImage=p.image||'';state.round.prize=[ord,p.title,p.description].filter(Boolean).join(' · ');state.round.reveal=p.showPublic!==false;const field=document.getElementById('roundPrize');if(field)field.value=state.round.prize;const reveal=document.getElementById('prizeReveal');if(reveal)reveal.value=state.round.reveal?'yes':'no';}
function winner(){return typeof state!=='undefined'?(state.winners||[]).find(w=>w.roundName===state.round?.name)||null:null;}
function visual(image,showImage=true){return `<div class="prize-ring">${image&&showImage?`<img src="${image}" alt="Premio">`:`<span class="gift">${showImage?'🏆':'🎁'}</span>`}</div>`;}
function hideRedundantGameFields(){['roundPrize','prizeReveal','roundStatus'].forEach(id=>{const e=document.getElementById(id);const label=e?.closest('label');if(label)label.classList.add('prize-hide-label');});}
function paintAdmin(){if(IS_PUBLIC||typeof state==='undefined')return;hideRedundantGameFields();const wrap=document.getElementById('roundPrizeSelectorWrap');if(!wrap)return;let box=document.getElementById('adminRoundPrizeTrophy');if(!box){box=document.createElement('div');box.id='adminRoundPrizeTrophy';box.className='prize-trophy-admin';wrap.after(box);}const p=info();box.innerHTML=`${visual(p.image,true)}<div><div class="prize-trophy-label">🏆 Premio seleccionado para esta ronda</div><div class="prize-trophy-title">${esc(p.title)}</div>${p.description?`<div class="prize-trophy-desc">${esc(p.description)}</div>`:''}<div class="prize-trophy-desc">${p.showPublic?'Visible mientras se juega':'Sorpresa hasta entregarlo'}</div></div><div class="prize-trophy-icon">🏆</div>`;}
function matchPrize(text){text=String(text||'').toLowerCase();return Array.isArray(state?.prizes)?state.prizes.find(p=>p.title&&text.includes(String(p.title).toLowerCase())):null;}
function awarded(){const out=[];for(const w of state?.winners||[]){const p=matchPrize(w.prize);if(!p||out.some(x=>x.p.id===p.id))continue;out.push({p,w});}return out;}
function paintPublic(){if(!IS_PUBLIC||typeof state==='undefined')return;const parent=document.getElementById('publicPrize')?.parentElement;if(!parent)return;const p=info(),w=winner(),won=!!w&&state.round?.status==='closed',show=!!state.round?.reveal||won;const old=document.getElementById('publicPrize');if(old)old.style.display='none';const oldPrice=document.getElementById('publicPrice');if(oldPrice){oldPrice.style.display='none';if(oldPrice.nextElementSibling)oldPrice.nextElementSibling.style.display='none';let price=document.getElementById('publicPriceV2');if(!price){price=document.createElement('div');price.id='publicPriceV2';price.className='public-price-v2';oldPrice.after(price);}price.innerHTML=`<strong>Combo 2 × ${shortMoney(state.settings.publicComboPrice||50000)}</strong> · Individual ${shortMoney(state.settings.publicSinglePrice||30000)}`;}
 let box=document.getElementById('publicRoundPrizeTrophy');if(!box){box=document.createElement('div');box.id='publicRoundPrizeTrophy';box.className='prize-trophy-public';parent.appendChild(box);}box.innerHTML=`${visual(p.image,show)}<div><div class="prize-trophy-label">🎯 ${esc(state.round?.name||'Ronda')} · Premio actual</div><div class="prize-trophy-title">${esc(show?p.title:'PREMIO SORPRESA')}</div><div class="prize-trophy-desc">${show&&p.description?esc(p.description):'Se revelará cuando corresponda ✨'}</div></div><div class="prize-trophy-icon">🏆</div>`;
 let past=document.getElementById('publicAwardedPrizes');if(!past){past=document.createElement('div');past.id='publicAwardedPrizes';past.className='awarded-prizes';parent.appendChild(past);}const done=awarded();past.innerHTML=done.length?`<div class="awarded-prizes-title">🏆 PREMIOS YA ENTREGADOS</div><div class="awarded-track">${done.map(({p,w})=>`<div class="awarded-card">${visual(p.image,true)}<div><b>${esc(p.title||'Premio')}</b><small>${esc(w.roundName||'Ronda')} · entregado</small></div></div>`).join('')}</div>`:'';
 const host=document.getElementById('publicWinner');if(host&&won)host.innerHTML=`<div class="winner-prize-moment">${p.image?`<img src="${p.image}" alt="${esc(p.title)}">`:'<div class="winner-gift">🏆</div>'}<div><div class="prize-trophy-label">✨ GANADOR DE LA RONDA</div><div class="winner-prize-name">${esc(w.buyer||w.cardId)}</div><div class="winner-prize-award">🏆 Se lleva: ${esc(p.title)}</div>${p.description?`<div class="prize-trophy-desc" style="margin-top:4px">${esc(p.description)}</div>`:''}<div class="winner-prize-card">Cartón ${esc(w.cardId)} · ${esc(state.round?.name||'Ronda')}</div></div></div>`;}
function enhance(){persistPrize();paintAdmin();paintPublic();}
document.addEventListener('change',e=>{if(e.target?.id==='roundPrizeSelect'){persistPrize();if(typeof saveState==='function')saveState();paintAdmin();paintPublic();}});
const saveBtn=document.getElementById('saveRoundBtn');saveBtn?.addEventListener('click',persistPrize,true);saveBtn?.addEventListener('click',()=>setTimeout(()=>{persistPrize();if(typeof saveState==='function')saveState();paintAdmin();},0));
if(typeof window.usePrizeInRound==='function'){const base=window.usePrizeInRound;window.usePrizeInRound=function(id){const r=base(id);const p=prizeById(id);if(p&&typeof state!=='undefined'){state.round.prizeImage=p.image||'';state.round.prizeTitle=p.title||'';state.round.prizeDescription=p.description||'';state.round.reveal=p.showPublic!==false;}setTimeout(enhance,0);return r;};}
if(typeof renderGame==='function'){const baseGame=renderGame;renderGame=function(){baseGame();paintAdmin();};}
if(typeof renderPublic==='function'){const basePublic=renderPublic;renderPublic=function(){basePublic();paintPublic();};}
installCss();setTimeout(enhance,120);
})();